include "WistTime.dll"
include "WistIO.dll"
include "List.wist"

func Start() {
    times = new List()

    for i = 0; i < 7; i = i + 1 {
        t = GetTimeInMs()
        
        // it's block works quite fast now!
        for j = 0; j < 500_000; j = j + 1 {
            l = [1,2,3,j]
        }
        
        
        times.Add(GetTimeInMs() - t)
        Print(l)
        Print(times.ToString())
    }
    
    Print(times.ToString())
    Print(times.Average())
	InputString()
}

