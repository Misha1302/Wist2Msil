include "WistTime.dll"
include "WistIO.dll"
include "List.wist"

func Start() {
    times = new List()

    for i = 0; i < 7; i = i + 1 {
        t = GetTimeInMs()
        
        for j = 0; j < 500_000; j = j + 1 {
            // it's very slow...
            lst = new List([])
        }
        
        
        times.Add(GetTimeInMs() - t)
        Print(times.GetElem(times.GetLen()))
    }
    
    Print(times.ToString())
    Print(times.Average())
}
