func Start() {
    a = new Vector3().Constructor()
    a.WriteSelf()
    
    ret a.x * a.y * a.z
}

func Write(a, b, c): 
    Print(ToStr(a) + " " + ToStr(b) + " " + ToStr(c))


struct Vector3(x, y, z) {
    func Constructor(self) {
        self.x = -1
        self.y = -2
        self.z = -3
        
        ret self
    }

    func WriteSelf(self):
        Print(ToStr(self.x) + " " + ToStr(self.y) + " " + ToStr(self.z))
}