func Start() {
    a = 1
    
startLabel:
    Write(a, a + 1, a + 2)
    a = a + 3
    
    if a <= 100:
        jmp startLabel
    
    ret 0
}

func Write(a, b, c): Print(ToStr(a) + " " + ToStr(b) + " " + ToStr(c))